---
phase: quick
plan: 260331-jx0
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
  - src/components/react/PipelineDiagram.tsx
autonomous: true
must_haves:
  truths:
    - "Projects section header (// projects + Projects heading) aligns with About and Experience sections"
    - "BeatProfiler case study content renders correctly under the Projects heading"
    - "Pipeline diagram shows step 0 (Video Input) before the existing 5 steps"
    - "Step 0 is visually distinct as an optional/alternate input path"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Projects section with aligned header"
    - path: "src/components/react/PipelineDiagram.tsx"
      provides: "6-step pipeline with step 0 for video input"
  key_links: []
---

<objective>
Fix the BeatProfiler case study's alignment under the Projects section heading, and add an optional step 0 (Video Input) to the Processing Pipeline diagram.

Purpose: The Projects section currently uses a custom dual-container layout that may cause visual misalignment of the heading with other sections. Additionally, the pipeline is missing the video contractility input path that BeatProfiler supports.

Output: Properly aligned Projects section + 6-step pipeline (00–05)
</objective>

<context>
@src/pages/index.astro
@src/components/react/PipelineDiagram.tsx
@src/components/Section.astro
@src/components/Container.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Projects section alignment in index.astro</name>
  <files>src/pages/index.astro</files>
  <action>
The Projects section (lines 127-215) manually creates two containers — header at standard width (`lg:max-w-[960px] xl:max-w-[1120px]`) and content at wider width (`lg:max-w-[1120px] xl:max-w-[1280px]`). This dual-container approach was intentional (per D-01 — pipeline needs ~900px for 5 horizontal steps), but review the current alignment:

1. Compare the Projects header container classes against Container.astro (`mx-auto w-full px-4 md:px-8 lg:max-w-[960px] xl:max-w-[1120px]`) — they should match exactly so the "// projects" label and "Projects" heading left-align with About/Experience headings.

2. Verify the header container div (line 129) matches Container.astro exactly. Currently it does: `mx-auto w-full px-4 md:px-8 lg:max-w-[960px] xl:max-w-[1120px]` — confirm this is correct.

3. If alignment is already correct, the issue may be that the wider content container causes the BeatProfiler content to appear misaligned. Check if the content container (line 139: `lg:max-w-[1120px] xl:max-w-[1280px]`) should use the same left padding so content starts at the same left edge.

4. Ensure the section uses consistent padding classes (`py-12 md:py-16`) matching Section.astro.

Key: The header must start at the same horizontal position as other sections' headers. The content below can be wider but should start from the same left edge.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Projects section "// projects" label and "Projects" heading visually align with "// about" and "// experience" headings at all breakpoints</done>
</task>

<task type="auto">
  <name>Task 2: Add step 0 (Video Input) to PipelineDiagram</name>
  <files>src/components/react/PipelineDiagram.tsx</files>
  <action>
Add a new step 0 at the beginning of the `steps` array in PipelineDiagram.tsx:

1. Insert new step at index 0:
   - id: 0
   - name: "Video Input"
   - description: "Contractile motion videos of beating cardiomyocytes"
   - icon: Create a simple video/camera SVG icon (20x20 viewBox, matching existing icon style — `fill="none" stroke="currentColor" strokeWidth="1.5"`)
     Suggested icon: a rectangle with a play triangle inside, or a simple film frame

2. Update the step number display — currently uses `String(step.id).padStart(2, '0')` which will naturally show "00" for id:0. This is fine.

3. The existing steps keep their current ids (1-5). Do NOT renumber them — step 0 is conceptually an optional alternate input path before the main signal processing begins.

4. Consider the layout impact: 6 steps + 5 arrows on desktop. The wider container (`xl:max-w-[1280px]`) should accommodate this. If the cards feel cramped, reduce `max-w-[160px]` to `max-w-[140px]` on each step card, or let the layout naturally wrap.

5. Both the reduced-motion and animated variants need the new step — the step is in the shared `steps` array so both paths will automatically pick it up.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Pipeline diagram renders 6 steps (00 Video Input → 01 Raw Signal Input → 02 Signal Processing → 03 Beat Detection → 04 Feature Extraction → 05 ML Classification) with arrows between each</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- Dev server (`npm run dev`) shows Projects heading aligned with About/Experience
- Pipeline diagram shows all 6 steps with proper numbering (00-05)
- Both light and dark themes render correctly
- Mobile layout stacks steps vertically with down arrows
</verification>

<success_criteria>
- Projects section header aligns horizontally with other section headers
- Pipeline diagram includes Video Input as step 00
- Build passes cleanly
- No visual regressions in either theme
</success_criteria>

<output>
After completion, create `.planning/quick/260331-jx0-fix-beatprofiler-alignment-under-project/260331-jx0-SUMMARY.md`
</output>
