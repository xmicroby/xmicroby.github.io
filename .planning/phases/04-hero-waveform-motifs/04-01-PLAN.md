---
phase: 04-hero-waveform-motifs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - tsconfig.json
  - package.json
  - src/components/react/TerminalHero.tsx
  - src/pages/index.astro
autonomous: true
requirements: [HERO-01, HERO-02, HERO-04, HERO-05]

must_haves:
  truths:
    - "Hero displays 'Youngbin Kim' via a typewriter animation that types out character by character on page load"
    - "Hero displays 'LLM Engineer / Data Scientist (Clinical AI)' typed after the name with a slight delay"
    - "A blinking block cursor (█) follows the typing position in the site's accent color"
    - "Full hero text is readable by screen readers and present in SSR HTML for search engines"
    - "'View my work' call-to-action button is visible below the typed text, linking to #projects"
    - "When prefers-reduced-motion is enabled, all text shows immediately without animation"
  artifacts:
    - path: "src/components/react/TerminalHero.tsx"
      provides: "Accessible typewriter animation component"
      min_lines: 40
    - path: "astro.config.mjs"
      provides: "React integration configuration"
      contains: "react()"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/react/TerminalHero.tsx"
      via: "Astro island with client:load directive"
      pattern: "TerminalHero.*client:load"
    - from: "src/components/react/TerminalHero.tsx"
      to: "CSS custom properties"
      via: "var(--color-accent) for cursor color"
      pattern: "var\\(--color-accent\\)"
---

<objective>
Set up Astro's React integration and build an accessible terminal-style typewriter hero component that types out Youngbin's name and title on page load.

Purpose: The hero is the first thing visitors see — the typing animation creates an immediate, memorable impression that signals "engineer" while remaining fully accessible to screen readers and search engines (HERO-01, HERO-02, HERO-04, HERO-05).

Output: A working TerminalHero React island that replaces the current static hero content with an animated typewriter effect plus blinking cursor.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hero-waveform-motifs/04-CONTEXT.md
@.planning/phases/04-hero-waveform-motifs/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/pages/index.astro (current hero to replace, lines 14-29):
```html
<section id="home" class="min-h-screen flex items-center justify-center">
  <Container>
    <div class="text-center md:text-left">
      <h1 class="text-[var(--color-text)]">Youngbin Kim</h1>
      <p class="font-mono text-sm md:text-base text-[var(--color-text-muted)] mt-4 tracking-[0.04em]">
        LLM Engineer / Data Scientist (Clinical AI)
      </p>
      <a href="#projects" class="inline-block mt-8 font-mono text-sm text-[var(--color-accent)] ...">
        View my work
      </a>
    </div>
  </Container>
</section>
```

From astro.config.mjs (current config to modify):
```js
import { defineConfig } from 'astro/config';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  site: 'https://youngbinkim.com',
  vite: {
    plugins: [tailwindcss()],
  },
});
```

From tsconfig.json (needs JSX config added):
```json
{ "extends": "astro/tsconfigs/strict" }
```

Design tokens used in hero (from src/styles/global.css):
- --color-text: main text color (both themes)
- --color-text-muted: subtitle color
- --color-accent: teal accent (#0d9488 light / #4ecdc4 dark) — cursor color per D-03
- --color-accent-hover: hover state for CTA button

Fonts: Inter (--font-sans) for body, JetBrains Mono (--font-mono) for monospace accents.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React integration and configure Astro</name>
  <files>package.json, astro.config.mjs, tsconfig.json</files>
  <read_first>
    - astro.config.mjs (current config — need to add react integration)
    - tsconfig.json (current config — need to add JSX settings)
    - package.json (current dependencies)
  </read_first>
  <action>
    1. Run `npx astro add react --yes` to install @astrojs/react, react, react-dom and auto-configure.
       - If the automatic CLI fails, manually install: `npm install @astrojs/react react react-dom`
       - Manually update astro.config.mjs: add `import react from '@astrojs/react';` and add `react()` to the `integrations` array
       - Manually update tsconfig.json: add `"compilerOptions": { "jsx": "react-jsx", "jsxImportSource": "react" }`
    2. Also install the animation library: `npm install motion` (Framer Motion v12+ — needed for Plan 02, installing now to avoid context switch later)
    3. Create the directory `src/components/react/` for React island components.
    4. Verify the build still passes: run `npm run build` and confirm zero errors.

    Per 04-RESEARCH.md: @astrojs/react v5 works with React 19 and Astro 6. The tsconfig JSX settings are required for .tsx files.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@astrojs/react"' package.json` exits 0
    - `grep -q '"react"' package.json` exits 0
    - `grep -q '"react-dom"' package.json` exits 0
    - `grep -q '"motion"' package.json` exits 0
    - `grep -q 'react()' astro.config.mjs` exits 0
    - `grep -q 'react-jsx' tsconfig.json` exits 0
    - `test -d src/components/react` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    - @astrojs/react, react, react-dom, and motion are in package.json dependencies
    - astro.config.mjs imports and uses the react() integration
    - tsconfig.json has jsx and jsxImportSource settings
    - `npm run build` succeeds with zero errors
    - src/components/react/ directory exists
  </done>
</task>

<task type="auto">
  <name>Task 2: Build TerminalHero React component and integrate into index.astro</name>
  <files>src/components/react/TerminalHero.tsx, src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (current hero section lines 14-29 — must understand existing markup to replace correctly)
    - src/styles/global.css (design tokens: --color-accent, --color-text, --color-text-muted, font families, reduced motion rule)
    - .planning/phases/04-hero-waveform-motifs/04-CONTEXT.md (D-01 through D-03 locked decisions)
    - .planning/phases/04-hero-waveform-motifs/04-RESEARCH.md (accessible typewriter pattern, section 1)
  </read_first>
  <action>
    Create `src/components/react/TerminalHero.tsx` — an accessible typewriter hero component:

    **Typing animation logic (per D-01, D-02):**
    - Two lines to type: "Youngbin Kim" (line 1) and "LLM Engineer / Data Scientist (Clinical AI)" (line 2)
    - Type line 1 first, then after a brief pause (~400ms), type line 2
    - Typing speed: ~80ms per character with slight random variance (±20ms) for natural feel
    - No fake terminal init commands (per D-02) — clean, direct typewriter

    **Blinking cursor (per D-03):**
    - Block cursor character `█` rendered after the last typed character
    - Cursor blinks via CSS animation: opacity toggles between 1 and 0 at ~530ms interval
    - Cursor color: `var(--color-accent)` — matches existing teal accent, not a new color
    - After typing completes, cursor continues to blink in place

    **Accessibility (HERO-05):**
    - The component's root container has `aria-label="Youngbin Kim — LLM Engineer / Data Scientist (Clinical AI)"`
    - The animated text spans use `aria-hidden="true"` so screen readers get the full label, not partial mid-animation text
    - Full text is rendered in the HTML markup (not injected via JS) — the component renders all text in JSX, using CSS/state to control visibility
    - For SEO: Astro will SSR the React component. Since the initial state shows no characters, wrap the full text in a `<noscript>` or use an sr-only span with the full text so crawlers always see it. RECOMMENDED: Use a visually-hidden `<h1>` with full text for SEO, and an `aria-hidden` animated div for the visual effect.

    **Reduced motion:**
    - Check `window.matchMedia('(prefers-reduced-motion: reduce)')` on mount
    - If reduced motion: set charIndex to max immediately (show all text, no animation)

    **Call-to-action (HERO-04):**
    - Below the typed text, render "View my work" link (`<a href="#projects">`)
    - Style it identically to the current CTA: `font-mono text-sm text-[var(--color-accent)] border border-[var(--color-accent)] px-6 py-3 rounded`
    - The CTA fades in (opacity 0→1 over 500ms) after typing completes
    - If reduced motion: CTA is visible immediately

    **Styling:**
    - Use the same layout classes as the current hero: centered on mobile (`text-center`), left-aligned on md+ (`md:text-left`)
    - h1 uses the existing typography hierarchy (40px desktop from global.css)
    - Subtitle uses `font-mono text-sm md:text-base text-[var(--color-text-muted)]` matching current

    **Integration in index.astro:**
    - Import TerminalHero at the top of the frontmatter
    - Replace the current `<section id="home">` content with `<TerminalHero client:load />`
    - Keep the `<section id="home" class="min-h-screen flex items-center justify-center">` wrapper and `<Container>` in the Astro template (not inside React) — the React component only handles the text content
    - This means the TerminalHero component renders a `<div>` with the h1, subtitle, cursor, and CTA — no section/container wrappers

    Important: Do NOT use typed.js or any external typewriter library. Build this custom per research findings (~50-70 lines of React).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/react/TerminalHero.tsx` exits 0
    - `grep -q 'TerminalHero' src/pages/index.astro` exits 0
    - `grep -q 'client:load' src/pages/index.astro` exits 0
    - `grep -q 'aria-hidden' src/components/react/TerminalHero.tsx` exits 0
    - `grep -q 'aria-label' src/components/react/TerminalHero.tsx` exits 0
    - `grep -q 'prefers-reduced-motion' src/components/react/TerminalHero.tsx` exits 0
    - `grep -q '#projects' src/components/react/TerminalHero.tsx` exits 0
    - `grep -q 'var(--color-accent)' src/components/react/TerminalHero.tsx` exits 0
    - `npm run build` exits 0
    - `grep -q 'Youngbin Kim' dist/index.html` exits 0 (text present in SSR output)
  </acceptance_criteria>
  <done>
    - TerminalHero.tsx exists with typewriter logic, blinking cursor, and CTA
    - index.astro uses `<TerminalHero client:load />` inside the #home section
    - `npm run build` succeeds
    - Component renders full text in aria-label for accessibility
    - Cursor uses var(--color-accent) color
    - CTA links to #projects
    - Reduced motion check skips animation
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Built HTML in `dist/index.html` contains the hero text (grep for "Youngbin Kim" and "LLM Engineer")
3. Built HTML contains `aria-label` with full hero text
4. The TerminalHero component file exists at src/components/react/TerminalHero.tsx
5. astro.config.mjs includes `react()` in integrations
</verification>

<success_criteria>
- Hero section has a working typewriter animation that types "Youngbin Kim" then "LLM Engineer / Data Scientist (Clinical AI)" on page load
- Blinking block cursor follows the text in the site's teal accent color
- Full text is accessible to screen readers (aria-label) and search engines (in SSR HTML)
- "View my work" CTA links to #projects and appears after typing
- prefers-reduced-motion skips animation and shows all content immediately
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-hero-waveform-motifs/04-01-SUMMARY.md`
</output>
