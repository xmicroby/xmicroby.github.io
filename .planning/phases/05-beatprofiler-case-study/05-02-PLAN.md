---
phase: 05-beatprofiler-case-study
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/index.astro
autonomous: false
requirements:
  - PROJ-01
  - PROJ-04
  - PROJ-05

must_haves:
  truths:
    - "BeatProfiler is presented as a deep-dive case study with narrative structure, not a generic project card"
    - "Impact metrics (521+ downloads, Harvard/MIT/Cornell/Columbia adoption, IEEE publication) are visually prominent as pills/tags"
    - "Links to beatprofiler.github.io and the IEEE paper are visible and functional"
    - "The pipeline diagram and cardiac waveform components are embedded and interactive"
    - "Case study layout breaks out of the narrow Container for visual breathing room"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "BeatProfiler case study section replacing placeholder"
      contains: "BeatProfiler"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/react/PipelineDiagram.tsx"
      via: "import and client:visible Astro island"
      pattern: "PipelineDiagram.*client:visible"
    - from: "src/pages/index.astro"
      to: "src/components/react/CardiacWaveform.tsx"
      via: "import and client:visible Astro island"
      pattern: "CardiacWaveform.*client:visible"
    - from: "src/pages/index.astro"
      to: "https://beatprofiler.github.io"
      via: "anchor tag href"
      pattern: "href=\"https://beatprofiler.github.io\""
    - from: "src/pages/index.astro"
      to: "IEEE paper URL"
      via: "anchor tag href"
      pattern: "href=.*ieee"
---

<objective>
Assemble the BeatProfiler case study section in the main page, replacing the "coming soon" placeholder with a compelling narrative layout featuring the pipeline diagram, cardiac waveform, impact metrics, and external links.

Purpose: This transforms the Projects section from a placeholder into the site's technical showcase — the piece that demonstrates depth beyond a resume. Per D-01, the layout breaks out of the narrow Container to give visualizations room. Per D-03, impact metrics are minimal pills/tags.

Output: Complete BeatProfiler case study visible on the live site, ready for visual verification.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-beatprofiler-case-study/05-CONTEXT.md
@.planning/phases/05-beatprofiler-case-study/05-01-SUMMARY.md

<interfaces>
<!-- Components created by Plan 01 that this plan consumes -->

From src/components/react/PipelineDiagram.tsx (Plan 01):
```typescript
export default function PipelineDiagram(): JSX.Element
// Interactive 5-step pipeline diagram with Framer Motion animations
// Use as: <PipelineDiagram client:visible />
```

From src/components/react/CardiacWaveform.tsx (Plan 01):
```typescript
export default function CardiacWaveform(): JSX.Element
// SVG cardiac waveform with animated path drawing
// Use as: <CardiacWaveform client:visible />
```

From src/components/Section.astro:
```astro
interface Props {
  id: string;    // Section anchor ID
  label: string; // Monospace label (e.g., "// projects")
  title: string; // Section heading
}
// Wraps content in Container (max-w ~960-1120px)
```

From src/components/Container.astro:
```astro
<!-- mx-auto w-full px-4 md:px-8 lg:max-w-[960px] xl:max-w-[1120px] -->
```

From src/components/react/ScrollReveal.tsx:
```typescript
export default function ScrollReveal({ children, delay, className }: {
  children: ReactNode; delay?: number; className?: string;
}): JSX.Element
// Use as: <ScrollReveal client:visible delay={N}>
```

From src/pages/index.astro (current Projects section to replace — lines 124-128):
```astro
<Section id="projects" label="// projects" title="Projects">
  <ScrollReveal client:visible>
    <p class="text-[var(--color-text-muted)]">This section is coming soon.</p>
  </ScrollReveal>
</Section>
```

From src/styles/global.css (design tokens):
```css
--color-accent: #0d9488 / #4ecdc4;
--color-text: #111827 / #e8edf5;
--color-text-muted: #6b7280 / #8892a8;
--color-surface-elevated: #f0f4f7 / #1a2340;
--color-border: #d4dde6 / #1e2d4a;
--color-bg: #f8fafb / #0a0e17;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build BeatProfiler case study section in index.astro</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (current full file — understand import structure and the Projects placeholder at lines 124-128)
    - src/components/Section.astro (Section component API — wraps content in Container)
    - src/components/Container.astro (Container width constraints — max-w-[960px] / max-w-[1120px])
    - src/components/react/PipelineDiagram.tsx (verify export and component API)
    - src/components/react/CardiacWaveform.tsx (verify export and component API)
    - src/components/react/ScrollReveal.tsx (scroll reveal API — delay prop)
    - .planning/phases/05-beatprofiler-case-study/05-CONTEXT.md (D-01: wide layout, D-03: minimal metrics)
  </read_first>
  <action>
Replace the Projects placeholder section (lines 124-128) in `src/pages/index.astro` with a full BeatProfiler case study. This is the main assembly task.

**Step 1: Add imports** at the top of the frontmatter block:
```astro
import PipelineDiagram from '../components/react/PipelineDiagram';
import CardiacWaveform from '../components/react/CardiacWaveform';
```

**Step 2: Replace the Projects section** (lines 124-128, the `<Section id="projects">...</Section>` block) with the following structure:

```astro
<!-- BeatProfiler Case Study -->
<section id="projects" class="py-12 md:py-16">
  <!-- Section header inside standard container -->
  <div class="mx-auto w-full px-4 md:px-8 lg:max-w-[960px] xl:max-w-[1120px]">
    <span class="font-mono text-sm text-[var(--color-accent)] tracking-[0.04em] leading-[1.5]">
      // projects
    </span>
    <h2 class="text-[var(--color-text)] mt-2 mb-8">
      Projects
    </h2>
  </div>

  <!-- Case study content in wider container (per D-01) -->
  <div class="mx-auto w-full px-4 md:px-8 lg:max-w-[1120px] xl:max-w-[1280px]">
    
    <!-- Case Study Header -->
    <ScrollReveal client:visible delay={0}>
      <div class="mb-8">
        <div class="flex flex-wrap items-center gap-3 mb-3">
          <h3 class="text-xl md:text-2xl font-bold text-[var(--color-text)]">BeatProfiler</h3>
          <span class="font-mono text-xs px-2 py-1 rounded bg-[var(--color-surface-elevated)] border border-[var(--color-border)] text-[var(--color-accent)]">Open Source</span>
        </div>
        <p class="text-base text-[var(--color-text-muted)] leading-relaxed max-w-3xl">
          An end-to-end machine learning platform for automated cardiac signal analysis. BeatProfiler takes raw calcium transient and field potential recordings from stem cell-derived cardiomyocytes, processes them through automated pipelines, and classifies disease phenotypes — turning hours of manual analysis into seconds.
        </p>
      </div>
    </ScrollReveal>

    <!-- Impact Metrics — minimal pills per D-03 -->
    <ScrollReveal client:visible delay={0.1}>
      <div class="flex flex-wrap gap-3 mb-10">
        <span class="inline-flex items-center gap-1.5 font-mono text-xs px-3 py-1.5 rounded-full bg-[var(--color-surface-elevated)] border border-[var(--color-border)] text-[var(--color-text)]">
          <span class="text-[var(--color-accent)]">↓</span> 521+ downloads
        </span>
        <span class="inline-flex items-center gap-1.5 font-mono text-xs px-3 py-1.5 rounded-full bg-[var(--color-surface-elevated)] border border-[var(--color-border)] text-[var(--color-text)]">
          <span class="text-[var(--color-accent)]">🏛</span> Harvard · MIT · Cornell · Columbia
        </span>
        <span class="inline-flex items-center gap-1.5 font-mono text-xs px-3 py-1.5 rounded-full bg-[var(--color-surface-elevated)] border border-[var(--color-border)] text-[var(--color-text)]">
          <span class="text-[var(--color-accent)]">📄</span> IEEE Published
        </span>
      </div>
    </ScrollReveal>

    <!-- Two-column layout: Pipeline + Waveform -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-10">
      
      <!-- Pipeline Diagram -->
      <ScrollReveal client:visible delay={0.15}>
        <div class="p-6 rounded-lg bg-[var(--color-surface-elevated)] border border-[var(--color-border)]">
          <p class="font-mono text-xs text-[var(--color-accent)] tracking-[0.08em] uppercase mb-4">Processing Pipeline</p>
          <PipelineDiagram client:visible />
        </div>
      </ScrollReveal>

      <!-- Cardiac Waveform -->
      <ScrollReveal client:visible delay={0.25}>
        <div class="p-6 rounded-lg bg-[var(--color-surface-elevated)] border border-[var(--color-border)]">
          <p class="font-mono text-xs text-[var(--color-accent)] tracking-[0.08em] uppercase mb-4">Calcium Transient Signal</p>
          <CardiacWaveform client:visible />
        </div>
      </ScrollReveal>
    </div>

    <!-- External Links -->
    <ScrollReveal client:visible delay={0.3}>
      <div class="flex flex-wrap gap-4">
        <a
          href="https://beatprofiler.github.io"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 font-mono text-sm text-[var(--color-accent)] hover:text-[var(--color-accent-hover)] transition-colors duration-200 border border-[var(--color-accent)] hover:border-[var(--color-accent-hover)] px-4 py-2 rounded"
        >
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <path d="M6 3H3v10h10v-3M9 2h5v5M14 2L7 9" />
          </svg>
          beatprofiler.github.io
        </a>
        <a
          href="https://ieeexplore.ieee.org/document/10288789"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 font-mono text-sm text-[var(--color-accent)] hover:text-[var(--color-accent-hover)] transition-colors duration-200 border border-[var(--color-accent)] hover:border-[var(--color-accent-hover)] px-4 py-2 rounded"
        >
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <path d="M4 2h8v12H4zM6 5h4M6 7.5h4M6 10h2" />
          </svg>
          IEEE Publication
        </a>
      </div>
    </ScrollReveal>

  </div>
</section>
```

**Key layout decisions explained:**
- The section header (`// projects` + `Projects`) stays inside the standard Container width for consistency with other sections
- The case study content uses a wider container (`lg:max-w-[1120px] xl:max-w-[1280px]`) per D-01 to give diagrams and visualizations room to breathe
- The two-column grid (`lg:grid-cols-2`) places Pipeline and Waveform side by side on desktop, stacked on mobile
- Impact metrics use `rounded-full` pills with monospace font per D-03 (not large stat cards)
- External links styled consistently with the hero CTA button pattern (border + accent color + monospace)
- Each subsection wrapped in `ScrollReveal` with incrementing delays for staggered "data loading" entrance (established Phase 4 pattern)

**Step 3: Verify the build succeeds:**
```bash
npm run build
```

**Important:** Do NOT modify the Section component itself. The case study section manually reproduces the Section header styling but uses a custom wider container for the content area. This preserves the existing Section component for other sections.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep "BeatProfiler" src/pages/index.astro && grep "beatprofiler.github.io" src/pages/index.astro && grep "ieee" src/pages/index.astro && grep "PipelineDiagram" src/pages/index.astro && grep "CardiacWaveform" src/pages/index.astro && grep "521" src/pages/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds with no errors
    - index.astro imports PipelineDiagram and CardiacWaveform at the top
    - The old "This section is coming soon" placeholder is GONE from index.astro
    - "BeatProfiler" appears in index.astro as an h3 heading
    - Case study content uses wider container: `xl:max-w-[1280px]` (or similar breakout)
    - Impact metric pills present: "521+ downloads", "Harvard · MIT · Cornell · Columbia", "IEEE Published"
    - `href="https://beatprofiler.github.io"` link present with `target="_blank"` and `rel="noopener noreferrer"`
    - IEEE paper link present with `href` containing "ieee" and `target="_blank"`
    - PipelineDiagram rendered as `<PipelineDiagram client:visible />`
    - CardiacWaveform rendered as `<CardiacWaveform client:visible />`
    - ScrollReveal wrappers present with incrementing delay values
    - Both visualization panels wrapped in elevated surface cards (`bg-[var(--color-surface-elevated)]`)
    - Monospace labels "Processing Pipeline" and "Calcium Transient Signal" present above the visualizations
  </acceptance_criteria>
  <done>BeatProfiler case study fully assembled in index.astro with narrative structure, impact pills, pipeline diagram, cardiac waveform, external links, and staggered scroll reveals — build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of BeatProfiler case study</name>
  <files>src/pages/index.astro</files>
  <action>
Human verification of the complete BeatProfiler case study. The agent automated all code in Task 1. Now the user verifies the visual and interactive result.

What was built:
- Narrative description of the project
- Interactive pipeline diagram (5 animated steps showing how BeatProfiler processes cardiac signals)
- SVG cardiac waveform visualization (animated path drawing from synthetic data)
- Impact metrics as minimal pills (521+ downloads, university adoption, IEEE)
- External links to beatprofiler.github.io and IEEE paper
- Staggered scroll-reveal entrance animations
- Works in both dark and light themes

Steps to verify:
1. Run `npm run dev` and open http://localhost:4321
2. Scroll down to the Projects section — verify it's no longer "coming soon"
3. Check the case study layout:
   - Header shows "BeatProfiler" with "Open Source" badge
   - Description paragraph explains what BeatProfiler does
   - Impact metric pills are visible and cleanly styled (not oversized cards)
4. Interact with the Pipeline Diagram:
   - Steps should animate in on scroll (or appear immediately if reduced motion)
   - Click/hover on a step to see its description expand
   - Verify all 5 steps are present and labeled correctly
5. Check the Cardiac Waveform:
   - SVG waveform should animate (draw from left to right) on scroll
   - Should look like authentic cardiac signal data (rapid upstroke, gradual decay)
   - Grid lines and axis labels visible
6. Verify links:
   - Click "beatprofiler.github.io" → opens in new tab
   - Click "IEEE Publication" → opens in new tab
7. Toggle dark/light mode — verify all elements look correct in both themes
8. Check mobile view (resize to ~375px width):
   - Pipeline and waveform stack vertically
   - Impact pills wrap cleanly
   - Everything readable and tappable

Resume signal: Type "approved" or describe issues to fix
  </action>
  <verify>User confirms visual verification by typing "approved"</verify>
  <done>User has verified that the BeatProfiler case study looks correct, is interactive, works in both themes, and is responsive on mobile</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- Projects section no longer shows "coming soon" placeholder
- BeatProfiler narrative, metrics, links, and visualizations all render
- Both React island components hydrate and animate on scroll
- Site works in both dark and light themes
- Responsive layout stacks on mobile, side-by-side on desktop
</verification>

<success_criteria>
- BeatProfiler case study replaces the placeholder with a compelling technical narrative (PROJ-01)
- Interactive pipeline diagram shows the 5-step processing flow (PROJ-02)
- SVG cardiac waveform renders from data array (PROJ-03)
- Impact metrics displayed as clean pills: 521+ downloads, university adoption, IEEE (PROJ-04)
- Links to beatprofiler.github.io and IEEE paper are visible and functional (PROJ-05)
- Human verification confirms the visual quality and interaction design
</success_criteria>

<output>
After completion, create `.planning/phases/05-beatprofiler-case-study/05-02-SUMMARY.md`
</output>
