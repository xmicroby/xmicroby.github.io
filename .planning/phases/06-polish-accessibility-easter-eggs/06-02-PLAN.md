---
phase: 06-polish-accessibility-easter-eggs
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: []
autonomous: false
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - EASTER-01
  - EASTER-02
  - EASTER-03

must_haves:
  truths:
    - "Site meets WCAG AA contrast in both dark and light modes (visual confirmation)"
    - "All animations stop completely with prefers-reduced-motion enabled"
    - "Social sharing preview shows correct title, description, and image"
    - "YNYC choir link is discoverable but subtle — doesn't scream 'link' at first glance"
    - "Biking/travel tooltip appears on hover"
    - "Easter eggs feel natural and don't break the professional flow"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 6 deliverables — accessibility, SEO meta tags, and easter egg discoverability.

Purpose: Confirm visual quality, interaction feel, and social sharing preview correctness that automated tests cannot fully validate.
Output: Human-verified final phase, ready for milestone completion.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-accessibility-easter-eggs/06-CONTEXT.md
@.planning/phases/06-polish-accessibility-easter-eggs/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and deploy for verification</name>
  <files></files>
  <read_first>
    - src/layouts/BaseLayout.astro (verify OG tags are present in source)
    - src/pages/index.astro (verify easter eggs are present in source)
  </read_first>
  <action>
    Run `npm run build` to generate the static site.
    Then run `npm run preview` (or `npx astro preview`) to start a local preview server.
    Report the local URL to the user for testing.

    Also verify the built HTML contains the expected meta tags:
    ```bash
    grep "og:title" dist/index.html
    grep "og:image" dist/index.html
    grep "twitter:card" dist/index.html
    grep "ynyc.org" dist/index.html
    ```
  </action>
  <verify>
    <automated>cd /home/youngbin/GitHub/xmicroby.github.io && npm run build 2>&1 | tail -3 && grep -c "og:title" dist/index.html && grep -c "ynyc.org" dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - dist/index.html contains `og:title`
    - dist/index.html contains `ynyc.org`
  </acceptance_criteria>
  <done>Site built and ready for local preview verification</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and interaction verification</name>
  <files></files>
  <action>
    Present the verification checklist below to the user. This is a human-verify checkpoint — no code changes, only visual/interaction confirmation.
  </action>
  <what-built>
    Phase 6 deliverables: WCAG AA accessibility, SEO meta tags, and three easter eggs.
  </what-built>
  <how-to-verify>
    **1. Easter Eggs (visit http://localhost:4321):**
    - Scroll to the About section
    - Find the last sentence: "When I'm not wrangling data..."
    - Hover over "Young New Yorkers' Chorus" — should show subtle underline, text turns teal on hover. Click it — should open ynyc.org in new tab.
    - Hover over "biking across the city" — should show a browser tooltip with bike emoji and route info. Dotted underline hint visible.
    - Hover over "planning my next trip" — should show a browser tooltip with plane emoji.
    - Confirm easter eggs look natural and don't disrupt the professional flow.

    **2. Reduced Motion (PERF-02):**
    - Open browser DevTools → Rendering → check "Emulate prefers-reduced-motion: reduce"
    - Reload the page
    - Verify: Hero text appears instantly (no typing animation), all sections visible immediately (no scroll reveals), no fade-in on page load
    - Toggle theme — confirm toggle still works but without transition animation

    **3. Both Themes:**
    - Check About section in light mode — text readable, easter egg underlines visible but subtle
    - Switch to dark mode — same checks
    - Confirm no broken colors or unreadable text anywhere

    **4. Social Sharing Preview:**
    - Open https://www.opengraph.xyz/ (or similar OG preview tool)
    - Enter https://youngbinkim.com
    - Verify: Title shows "Youngbin Kim — LLM Engineer / Data Scientist (Clinical AI)"
    - Verify: Description shows the portfolio description
    - Verify: Image shows the dark-themed OG card with name and title
    - (Note: This only works after deploying to GitHub Pages. If testing locally, verify meta tags exist in dist/index.html source instead.)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <verify>User types "approved"</verify>
  <done>Human confirms all Phase 6 deliverables: easter eggs are subtle and discoverable, reduced motion works, both themes pass visual inspection, OG meta tags verified</done>
</task>

</tasks>

<verification>
1. Human confirms easter eggs are discoverable but subtle
2. Human confirms reduced motion completely disables animations
3. Human confirms both themes look correct with no contrast issues
4. Human confirms OG meta tags are present (verified locally or via deployed site)
</verification>

<success_criteria>
- User approves visual quality of easter eggs
- User confirms reduced motion works as expected (per D-01: completely disabled)
- User confirms no accessibility regressions in either theme
- OG meta tags verified in built HTML
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-accessibility-easter-eggs/06-02-SUMMARY.md`
</output>
